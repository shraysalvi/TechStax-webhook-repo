# from flask_pymongo import PyMongo
# Setup MongoDB here
# mongo = PyMongo(uri="mongodb://localhost:27017/techstax")
# db = mongo.db
# print(mongo.db)
# This do not work because THis is old syntax and no longer working.
# Also this template generate circular import which leads to error.
